Week of Sept 8, 2026 — Shivani

- Split settings.py into cozyday/settings/ (base, development, production).
- Moved SECRET_KEY into .env via python-dotenv; added .env.example for teammates.
- Verified DEBUG/ALLOWED_HOSTS differ correctly between dev and prod.
- Next: build out docs/ and README, then merge section1-settings-security into main.

Note for team: after pulling this branch, you'll need to copy .env.example to .env
and add a real DJANGO_SECRET_KEY, or manage.py will raise an error.

Week of Sept 15, 2026 — Rishabh

- Built the four required "Views" for the assignment — four different ways
  to show a user's plan items on a page (fully manual, Django's normal
  shortcut, a written-out class-based version, and Django's automatic
  class-based version). Connected each to its own URL for testing.
- The page they show is just a plain placeholder for now — Lin is building
  the real, styled version next.
- Learned: writing the manual version first made it clearer what Django's
  shortcuts are actually doing behind the scenes.

Week of Sept 19, 2026 — Lin Gao
- Built the reusable template structure for the PlanItem pages.
- Created base.html with reusable title and content blocks, plus basic styling for a consistent CozyDay layout.
- Updated planner/planitem_list.html to extend base.html and display PlanItem objects using a loop.
- Added an empty-state message using {% empty %}: "No plan items available yet."
- Reused the same planner/planitem_list.html template across all four views because they all use the same context variable, "items".
- Tested both the normal list state and the empty state. After testing the empty state with an empty queryset, restored the view to PlanItem.objects.all().
- Challenge/Note: The main thing I needed to confirm was that all four views passed the same context variable name, which made template reuse straightforward.