# Branching Strategy

- `main` is always deployable; no one commits directly to it.
- Each person works on a feature branch named after their task (e.g. `shivani-technical-documentation`, `lin-admin`).
- Open a PR into `main` when a branch is ready; at least one teammate reviews before merging.
- Delete branches after merging to keep the branch list clean.